using UnityEngine;
namespace MyFramework{
    public class HideComponent : MonoBehaviourSimplify
    {
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {
            Hide();
        }

    }
}